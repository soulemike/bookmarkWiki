{ "$schema": "https://json-schema.org/draft/2020-12/schema", "title": "BookmarkQueueItem", "type": "object", "required": ["id", "url", "normalizedUrl", "originalTitle", "source", "status", "createdAt", "updatedAt", "attemptCount"], "properties": { "id": { "type": "string" }, "url": { "type": "string" }, "normalizedUrl": { "type": "string" }, "originalTitle": { "type": "string" }, "source": { "enum": ["current_tab", "context_menu", "bookmark_event", "bulk_import"] }, "status": { "enum": ["queued", "classified", "needs_review", "approved", "moved", "ignored", "archived", "error"] }, "attemptCount": { "type": "integer", "minimum": 0 } } }
