# Bookmark Queue Agent

Manifest V3 Chrome extension implementing the MVP 1 Bookmark Queue Agent spec.

## Development

```sh
cd extension
npm install
npm run typecheck
npm test
npm run build
```

The built unpacked extension is emitted to `extension/dist`.
